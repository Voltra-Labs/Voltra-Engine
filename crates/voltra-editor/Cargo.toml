[package]
name = "voltra-editor"
description = "Voltra editor application."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
voltra-assets.workspace = true
voltra-core.workspace = true
voltra-ecs.workspace = true
voltra-physics.workspace = true
voltra-render.workspace = true
voltra-scene.workspace = true
env_logger.workspace = true
log.workspace = true

# Only the tests need these: a component whose `Serialize` fails is the one real
# way to make a snapshot refuse, and `UnknownComponents` holds RON values.
[dev-dependencies]
ron.workspace = true
serde.workspace = true
